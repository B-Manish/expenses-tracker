
You are working inside my existing React + Vite expense tracker project.

Continue the implementation using the phase plan in the `implementation/` folder.

Instructions:

1. Read `implementation/progress.md`.
2. Find the first phase whose status is not `Completed`.
3. Read that phase's Markdown file completely.
4. Implement only that phase.
5. Before coding, update `implementation/progress.md` and mark that phase as `In Progress`.
6. Do not jump to later phases.
7. Do not implement work from future phases unless it is strictly required by the current phase acceptance criteria.
8. After finishing the phase, run relevant validation/tests from that phase file.
9. Update `implementation/progress.md`.
10. Mark the phase as `Completed` only if all acceptance criteria pass.
11. If blocked, mark the phase as `Blocked` and explain the blocker clearly in Notes.
12. Preserve Cloudflare free-tier compatibility.
13. Do not use a VM, VPS, paid backend, or paid database.
14. Do not expose secrets in React frontend code.
15. Do not expose Cloudflare D1 directly to the browser.
16. Do not implement future bank sync.
17. Do not implement direct SBI API calls.
18. Do not collect SBI username, password, OTP, net banking credentials, or bank credentials of any kind.
19. Future bank sync, if ever added later, must use an Account Aggregator-style consent flow.

Use `UPDATED_PROJECT_REQUIREMENTS.md` as the source of truth. If it is missing, use `PROJECT_REQUIREMENTS.md`.

At the end, summarize:

- Phase completed or blocked.
- Files changed.
- Validation/tests run.
- Any remaining notes or blockers.


